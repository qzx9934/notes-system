# 运行时依赖（版本范围锁定，避免被破坏性大版本影响）
flask>=3.1,<4.0
flask-cors>=6.0,<7.0
openpyxl>=3.1,<4.0
# AI 总结：调用 DeepSeek HTTP 接口
requests>=2.31,<3.0
# 生产部署用 WSGI 服务器（gunicorn wsgi:app）；便携启动脚本不依赖它
gunicorn>=21.2,<24.0
