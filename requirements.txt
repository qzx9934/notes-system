# 运行时依赖（版本范围锁定，避免被破坏性大版本影响）
flask>=3.1,<4.0
flask-cors>=6.0,<7.0
openpyxl>=3.1,<4.0
# Word 试卷导出
python-docx>=1.1,<2.0
# AI 总结：调用 DeepSeek HTTP 接口
requests>=2.31,<3.0
# 生产部署用 WSGI 服务器（gunicorn wsgi:app）；便携启动脚本不依赖它
gunicorn>=21.2,<24.0
# 图片压缩（上传时自动减小体积、保清晰度）；未安装时自动跳过压缩，不影响上传
# 上限放宽到 <13：兼容已广泛使用的 Pillow 12，避免在已装 12 的机器上被强制降级
Pillow>=10.0,<13.0
